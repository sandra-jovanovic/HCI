<UserControl x:Class="HCI_projekat.Wizard.SecondStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCI_projekat.Wizard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="50" Background="Wheat">
        <TextBlock FontSize="25" >Odaber pozicije</TextBlock>
        <Control Margin="0 5" />
        <TextBlock FontSize="18" >Odaberite Vaše radno mesto:</TextBlock>
        <Control Margin="0 5" />
        <RadioButton IsChecked="{Binding IsDoctor}">Lekar</RadioButton>
        <RadioButton IsChecked="{Binding IsMedicalTechnician}">Medicinski tehničar</RadioButton>
        <RadioButton IsChecked="{Binding IsSupportStaff}">Pomoćno osoblje</RadioButton>
    </StackPanel>
</UserControl>
